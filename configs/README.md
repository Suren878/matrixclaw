# Configs

This directory holds configuration-related assets for `matrixclaw`.

Typical contents:
- example config files
- safe development templates

Rules:
- do not put secrets in this directory
- keep examples safe to commit
- keep examples aligned with the JSON setup shape in `setup.example.json`
